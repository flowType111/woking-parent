spring:
  profiles:
    active: dev
  application:
    name: working-common-manage
  main:
    allow-bean-definition-overriding: true
  mvc:
    hidden-method:
      filter:
        enabled: true
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    password: 88888888

server:
  port: 8860
  max-http-header-size: 4048576
  tomcat:
    max-http-post-size: -1  #请求参数长度

#RSA加密
rsa:
  key:
    defaultPublicKey:
    defaultPrivateKey:

#文件上传
file:
  image:
    path: D:/working/file
    domain: D:/working/file
    prefix: /working/file

---
spring:
  profiles: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        username: nacos
        password: nacos
ribbon:
  eager-load:
    clients:


